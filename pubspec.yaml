name: smart_money_app
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.5.0

dependencies:
  shared_preferences:
  jwt_decoder:
  flutter:
    sdk: flutter
  flutter_swiper_view:
  flutter_expandable_fab:
  provider: ^6.0.0
  http: ^1.2.2
  intl: ^0.19.0
  status_alert: ^1.0.1
  fl_chart: ^0.69.0
  pie_chart: ^5.4.0
  google_fonts: ^6.2.1
  collection: ^1.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
